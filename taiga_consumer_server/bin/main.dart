import 'package:taiga_consumer_server/server.dart';

/// This is the starting point for your Serverpod server. Typically, there is
/// no need to modify this file.
void main(List<String> args) {
  // settings = config.json;
  // CronService()
  //   ..addCallback(settings.mailNotificationDuration, () => dispararProceso ('mailer_worker.dart'))
  //   ..addCallback(settings.mailNotificationDuration, () => dispararProceso ('mailer_worker.dart'))
  // ..run();
  run(args);
}
